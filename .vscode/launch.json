{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "chrome",
          "request": "launch",
          "name": "ng serve",
          "url": "http://localhost:4200",
          "preLaunchTask": "npm: start",
          "webRoot": "${workspaceFolder}"
      }
  ]
}
